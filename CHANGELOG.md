# 资产账本 PRO 变更日志

## v0.42 (2026-09-16)

收支统计视图标签改为「按月 / 前90天 / 前半年 / 前一年」，时间范围改为以当天为终点的滚动窗口。

---

### 变更

1. **视图标签重命名** — 「90天 / 半年 / 本年」→「前90天 / 前半年 / 前一年」，顺序保持按月在前。

### 修复

2. **「90天」标签与实际范围不符** — `90d` 模式在 `getStatsPeriodLabel()` 与 `logsForStatsPeriod()` 中均落入 `else` 分支，实际按 7 天/周计算，与标签「90天」不符。

---

### 时间范围定义

| 视图 | 原定义 | 新定义 |
|------|--------|--------|
| 按月 | 指定自然月 | 指定自然月（不变） |
| 前90天 | 7 天（周） | 当天往前 90 天滚动窗口 |
| 前半年 | 上半年 / 下半年 | 当天所在月起前 6 个日历月 |
| 前一年 | 自然年 | 当天所在月起前 12 个日历月 |
| 自定义 | 起止日 | 起止日（不变） |

导航 ‹ › 保留，按窗口长度整体回退 / 前移，标签显示实际日期区间。

实现上抽取 `getStatsWindow()` 统一产出 `[start, end)` 区间，标签与过滤共用，消除两处重复逻辑；日期解析统一为 `new Date(date + 'T00:00:00')`（本地零点），避免 `new Date('YYYY-MM-DD')` 按 UTC 解析在 UTC+8 下产生 8 小时偏差。

---

## v0.41 (2026-09-15)

修复账目流水明细未按时间排列的问题，编辑条目后自动重排。

---

### 修复

1. **流水明细未按时间排列** — `renderLedger()` 仅对分组键排序（最新分组在上），组内条目沿用 `logData` 数组顺序，未按日期排列；编辑条目日期后虽触发重绘，但条目在数组中的索引不变，位置不更新。修复方案：组内条目按日期倒序排序（同日按 `id` 倒序），确保始终最新在上，新增/编辑/删除后自动重排。

---

## v0.26 (2026-06-22)

新增支出分类。

---

### 新增

1. **支出分类** — 在支出下拉选项、分类徽标样式、分类颜色映射中新增分类支持。

---

## v0.24 (2026-06-02)

修复页面刷新后资产概览数据全部归零的关键问题。

---

### 修复

1. **页面刷新后资产概览数据全部归零** — 编辑弹窗输入框 `edit_amount` 的 HTML 定义在 `<script>` 标签之后，初始化脚本执行时该 DOM 元素尚不存在，`document.getElementById('edit_amount')` 返回 `null` 导致 `TypeError`，后续 `loadSavedData()` 函数无法执行。修复方案：延迟绑定 `edit_amount` 的输入事件监听器，确保 `loadSavedData()` 始终成功执行。

---

## v0.23 (2026-06-02)

修复资产概览数据刷新丢失与统计报表实时更新问题。

---

### 修复

1. **资产概览数据刷新丢失** — `localStorage.setItem` 添加 try-catch 错误保护，新增 `change` 事件作为备份保存触发点，避免 `input` 事件不可靠导致数据未持久化。`loadSavedData()` 添加空数据诊断日志。
2. **收支记录不实时更新统计报表** — `addLog()`、`deleteLog()`、`saveEditLog()` 补全 `renderStats()` 调用，确保收支记录操作后统计报表立即刷新。

---

## v0.20 (2026-05-31)

新增测试数据加载、可编辑记录、分类统计明细、德州K线图、自定义周期统计等功能。

---

### 一、文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `my_assets_v4.html` | ✏️ 更新 | 合并测试版全部功能 |
| `启动测试.bat` | 🆕 新增 | Windows 快捷启动脚本 |
| `migrate_category.js` | 🆕 新增 | 备份数据分类迁移脚本 |
| `README.md` | ✏️ 更新 | 补充项目说明与功能指南 |

---

### 二、标题与导航调整

| 位置 | 变更前 | 变更后 |
|------|--------|--------|
| 收支账本 | `收支账本` | `收支账本` + 备注 `实际出入帐记录` |
| 统计报表 | `收支统计` | `统计报表` + 备注 `按分类统计支出与收入` |
| 底部导航③ | `收支统计` | `统计报表` |
| 右上角 | 无 | `v0.20` |

---

### 三、功能新增

#### 3.1 🧪 测试数据加载

**位置**: 资产概览页底部

- **「加载测试数据」**按钮：一键生成近三个月约 100+ 条示例收支记录
  - 预填资产金额
  - 包含全部分类
- **「清空所有数据」**按钮：双重确认后重置 localStorage
- **自动引导**：首次打开无数据时弹窗询问是否加载测试数据

#### 3.2 🏛️ 新增收入分类

| 位置 | 说明 |
|------|------|
| 记账表单（收入模式） | 下拉新增分类选项 |
| `getCategoryBadge()` | 分类徽标样式 |
| `getCategoryColor()` | 分类颜色映射 |
| `inferCategory()` | 标题自动归类 |
| 测试数据 | 含示例记录 |

#### 3.3 📊 分类统计可点击查看明细

**位置**: 统计报表页 → 支出分类 / 收入分类

- 每个分类条目改为**可点击交互**（悬停灰底 + 选中蓝框高亮）
- 选中后在分类下方展开**明细面板**：
  - 按日期倒序显示该分类所有记录
  - 每条显示标题、日期、账户、金额
  - 顶部合计条数 + 总金额
- 再次点击取消选中，面板收起

#### 3.4 📅 自定义周期统计

**位置**: 统计报表页 → 周期切换

- 在「按月」「按周」旁新增 **「自定义」**按钮
- 切换后展开日期范围选择器：
  - 起始日期 / 结束日期 → 点击「确定」刷新
  - 选中自定义时隐藏前后导航按钮
- `logsForStatsPeriod()` 适配自定义范围过滤

#### 3.5 📈 德州 K线图（股票蜡烛图风格）

**位置**: 统计报表页 → 德州汇总 → 各期趋势

- 新增 **日K / 周K / 月K** 切换标签
- 按时间顺序**横向排列**蜡烛（`overflow-x: auto` 可滚动）
- 每根蜡烛：
  - 🔴 **红色阳线** = 盈利周期（从零线向上延伸）
  - 🟢 **绿色阴线** = 亏损周期（从零线向下延伸）
  - 柱体高度按盈亏金额比例缩放（1.25倍 headroom）
  - 上下影线 = 柱体高度的 30%
- 顶部信息条：总周期数、盈利次数、亏损次数、场均净盈亏
- 底部缩略标签：日K=`M/D`，周K=`M/D-M/D`，月K=`YY/MM`

---

### 四、收支记录编辑

#### 4.1 可编辑弹窗

每条收支记录右侧新增 **✎ 编辑按钮**（半透明，hover 高亮显示）。

| 可编辑字段 | 控件 |
|-----------|------|
| 事项说明 | 文本输入 |
| 金额 | 数字输入（仅正数，禁止科学计数法） |
| 日期 | 日期选择器 |
| 账户 | 下拉 |
| 收支类型 | 下拉（支出/收入，切换后自动更新分类选项） |
| 分类 | 下拉 |

保存后即时刷新列表和余额计算。

#### 4.2 防止数据污染

- `calculateAssets()` 跳过 `edit_` 前缀的输入框
- `edit_amount` 加入正数过滤

---

### 五、数据备份兼容

#### 5.1 分类迁移脚本

`migrate_category.js`：读取旧版备份 JSON，对每条记录运行 `inferCategory()` 补全 `category` 字段。

```bash
node migrate_category.js 输入文件.json 输出文件.json
```

#### 5.2 备份文件处理

备份 JSON 文件 → 记录全部补全分类。

---

### 六、CSS 新增样式

| 类名 | 用途 |
|------|------|
| `.kline-chart-wrap` | K线图横向滚动容器 |
| `.kline-chart` | K线图表 flex 布局 |
| `.kline-zero-line` | K线零轴参考线 |
| `.kline-bar-group` | 单根 K线柱组（48px 宽） |
| `.kline-candle-area` | 蜡烛定位区域（180px 高） |
| `.kline-candle` | 蜡烛实体 |
| `.kline-wick` | 蜡烛影线 |
| `.kline-up / .kline-down` | 阳线/阴线着色 |
| `.kline-label` | 底部周期标签（8px） |
| `.kline-vol` | 底部金额标签（7px） |
| `.kline-chart-info` | K线统计信息条 |
| `.edit-modal-overlay` | 编辑弹窗遮罩层 |
| `.edit-modal` | 编辑弹窗主体 |
| `.edit-btn` | 记录编辑按钮 |
| `.cat-detail-item` | 分类明细条目 |
| `.custom-date-row` | 自定义日期范围行 |

---

### 七、数据流

```
用户输入 → addLog() → logData[] → localStorage
                 ↓
           inferCategory() → category 自动推断
                 ↓
           renderLedger() → 按 月/周 分组展示
           renderStats()  → 分类统计 + 德州K线
           calculateLedgerTotal() → 差异计算
```

编辑流程：

```
✎按钮 → openEditModal(id) → 弹窗回填 → 用户修改 → saveEditLog()
  → 更新 logData + localStorage → renderLedger() + calculateLedgerTotal()
```

---

### 八、存储 Key

| Key | 用途 |
|-----|------|
| `asset_input_cache_v4` | 资产金额缓存 |
| `asset_ledger_logs_v4` | 收支记录 |
| `asset_view_mode_v4` | 视图偏好（月/周） |
| `asset_snapshots_v4` | 快照数据 |

---

### 九、依赖

- [Tailwind CSS CDN](https://cdn.tailwindcss.com) — 样式框架
- 浏览器 `localStorage` — 数据持久化（无后端）

---

### 十、后续计划

- [ ] 导出数据包含分类信息
- [ ] 收支记录搜索/筛选
- [ ] 图表导出为图片
- [ ] 多币种支持
- [ ] 移动端 PWA 离线缓存
